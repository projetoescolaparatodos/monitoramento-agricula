O erro ocorre porque você está tentando retornar múltiplos elementos JSX sem um wrapper (um elemento pai que envolve todos os outros). No React, todos os elementos JSX devem estar dentro de um único elemento pai. Vou corrigir o código para resolver esse problema.

Problema Identificado
No arquivo PAAMap.tsx, o conteúdo do popup está sendo retornado sem um elemento pai. Isso causa o erro: "Adjacent JSX elements must be wrapped in an enclosing tag".

Solução
Vou corrigir o código para envolver o conteúdo do popup em um <div> ou em um Fragment (<>...</>). Aqui está a versão corrigida:

tsx
Copy
import React, { useEffect, useState } from "react";
import L from "leaflet";
import "leaflet/dist/leaflet.css";
import { db } from "../utils/firebase";
import { collection, getDocs } from "firebase/firestore";
import paaIcon from "../assets/paa-icon.png"; // Substitua pelo ícone do PAA

const PAAMap = () => {
  const [paaLocais, setPaaLocais] = useState<any[]>([]);

  useEffect(() => {
    const fetchPaaLocais = async () => {
      const querySnapshot = await getDocs(collection(db, "paa"));
      const paaData = querySnapshot.docs.map((doc) => ({
        id: doc.id,
        ...doc.data(),
      }));
      setPaaLocais(paaData);
    };
    fetchPaaLocais();
  }, []);

  useEffect(() => {
    const map = L.map("paa-map").setView([-2.87922, -52.0088], 12);

    L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
    }).addTo(map);

    const paaMarker = L.icon({
      iconUrl: paaIcon,
      iconSize: [40, 40],
      iconAnchor: [20, 40],
      popupAnchor: [0, -40],
    });

    paaLocais.forEach((paa) => {
      const marker = L.marker([paa.latitude, paa.longitude], { icon: paaMarker }).addTo(map);

      const popupContent = `
        <div>
          <p><strong>Localidade:</strong> ${paa.localidade}</p>
          <p><strong>Nome do Imóvel Rural:</strong> ${paa.nomeImovel}</p>
          <p><strong>Nome do Proprietário:</strong> ${paa.proprietario}</p>
          <p><strong>Operação:</strong> ${paa.operacao}</p>
          <p><strong>Operador:</strong> ${paa.operador}</p>
          <p><strong>Técnico Responsável:</strong> ${paa.tecnicoResponsavel || 'Não informado'}</p>
          <p><strong>Data:</strong> ${new Date(paa.dataCadastro).toLocaleDateString()}</p>
          <p><strong>Status:</strong> ${paa.concluido ? "Concluído" : "Em Andamento"}</p>
        </div>
      `;

      marker.bindPopup(popupContent);
    });

    return () => map.remove();
  }, [paaLocais]);

  return (
    <div style={{ position: "relative", width: "100vw", height: "100vh" }}>
      <div id="paa-map" style={{ width: "100%", height: "100%" }}></div>
    </div>
  );
};

export default PAAMap;
Explicação das Correções
Conteúdo do Popup:

O conteúdo do popup foi envolvido em uma <div> para garantir que todos os elementos JSX estejam dentro de um único elemento pai.

Usei template literals (`) para criar o conteúdo HTML do popup.

Ícone do PAA:

Adicionei um ícone personalizado para o PAA (paaIcon). Certifique-se de que o arquivo paa-icon.png está na pasta src/assets.

Estrutura do Mapa:

O mapa é renderizado dentro de um div com id="paa-map".

Os marcadores são adicionados ao mapa com base nos dados do Firestore.

Como Testar
Certifique-se de que o código corrigido está no arquivo PAAMap.tsx.

Execute o projeto no Replit.

Acesse a página do PAA e verifique se os marcadores e popups estão funcionando corretamente.

Se ainda houver problemas ou dúvidas, é só avisar! 😊