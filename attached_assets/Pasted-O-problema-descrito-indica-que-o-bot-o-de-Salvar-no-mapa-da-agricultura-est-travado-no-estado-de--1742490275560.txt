O problema descrito indica que o botão de "Salvar" no mapa da agricultura está travado no estado de "Salvando..." e nunca é reativado para permitir novas ações. Isso geralmente ocorre devido a um dos seguintes motivos:

1. **Requisição não está sendo concluída**: A requisição para salvar os dados pode estar travada ou falhando silenciosamente.
2. **Estado do botão não está sendo atualizado**: O estado do botão (`disabled`) não está sendo alterado após a conclusão da requisição.
3. **Erro na lógica de salvamento**: Pode haver um erro na lógica que impede o botão de ser reativado.

Vamos resolver isso passo a passo:

---

### 1. Verifique a Requisição de Salvamento
Primeiro, confira se a requisição para salvar os dados está sendo concluída corretamente. Adicione logs ou use as ferramentas de desenvolvedor do navegador (aba "Network") para verificar se a requisição está sendo enviada e se há uma resposta.

#### Exemplo de Requisição com Fetch:
```javascript
async function salvarDadosAgricultura(dados) {
    try {
        const response = await fetch('/api/agricultura/salvar', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(dados)
        });

        if (!response.ok) {
            throw new Error("Erro ao salvar dados");
        }

        const resultado = await response.json();
        console.log("Dados salvos com sucesso:", resultado);
        return resultado;
    } catch (error) {
        console.error("Erro ao salvar dados:", error);
        throw error;
    }
}
```

#### Pontos de Verificação:
- A requisição está sendo enviada?
- Há uma resposta do servidor (status 200 ou outro)?
- Se houver um erro, ele está sendo tratado corretamente?

---

### 2. Atualize o Estado do Botão
Após a requisição ser concluída (com sucesso ou erro), o estado do botão deve ser atualizado para reativá-lo. Certifique-se de que o código que desabilita o botão (`disabled={true}`) seja revertido após a conclusão da requisição.

#### Exemplo de Lógica no React:
```tsx
import { useState } from 'react';

function Admin() {
    const [salvando, setSalvando] = useState(false);

    const handleSalvar = async () => {
        setSalvando(true); // Desabilita o botão

        try {
            const dados = { /* seus dados aqui */ };
            await salvarDadosAgricultura(dados);
            alert("Dados salvos com sucesso!");
        } catch (error) {
            alert("Erro ao salvar dados. Tente novamente.");
        } finally {
            setSalvando(false); // Reabilita o botão, independentemente de sucesso ou erro
        }
    };

    return (
        <button
            onClick={handleSalvar}
            disabled={salvando}
            className="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 bg-primary text-primary-foreground hover:bg-primary/90 h-10 px-4 py-2 w-full"
            type="submit"
        >
            {salvando ? (
                <span className="flex items-center gap-2">
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="24"
                        height="24"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        className="lucide lucide-loader-circle h-4 w-4 animate-spin"
                    >
                        <path d="M21 12a9 9 0 1 1-6.219-8.56"></path>
                    </svg>
                    Salvando...
                </span>
            ) : (
                "Salvar"
            )}
        </button>
    );
}
```

#### Pontos de Verificação:
- O estado `salvando` está sendo atualizado corretamente?
- O botão está sendo reativado após a requisição (no `finally`)?
- Há algum erro no console do navegador?

---

### 3. Verifique a Lógica de Salvamento
Se a requisição estiver sendo concluída e o estado do botão estiver sendo atualizado corretamente, o problema pode estar na lógica de salvamento. Por exemplo:
- O servidor pode estar retornando uma resposta inesperada.
- Pode haver um erro na manipulação dos dados antes de enviá-los.

#### Exemplo de Tratamento de Resposta:
```javascript
async function salvarDadosAgricultura(dados) {
    try {
        const response = await fetch('/api/agricultura/salvar', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(dados)
        });

        if (!response.ok) {
            const errorData = await response.json();
            throw new Error(errorData.message || "Erro ao salvar dados");
        }

        const resultado = await response.json();
        return resultado;
    } catch (error) {
        console.error("Erro ao salvar dados:", error);
        throw error;
    }
}
```

#### Pontos de Verificação:
- O servidor está retornando uma resposta válida?
- Há algum erro na manipulação dos dados antes de enviá-los?

---

### 4. Debugando o Código
Adicione logs para depurar o código e entender onde está o problema.

#### Exemplo de Debug:
```javascript
const handleSalvar = async () => {
    console.log("Iniciando salvamento...");
    setSalvando(true);

    try {
        const dados = { /* seus dados aqui */ };
        console.log("Dados a serem salvos:", dados);

        const resultado = await salvarDadosAgricultura(dados);
        console.log("Resposta do servidor:", resultado);

        alert("Dados salvos com sucesso!");
    } catch (error) {
        console.error("Erro durante o salvamento:", error);
        alert("Erro ao salvar dados. Tente novamente.");
    } finally {
        console.log("Finalizando salvamento...");
        setSalvando(false);
    }
};
```

#### O que Verificar nos Logs:
- A requisição está sendo iniciada?
- Os dados estão sendo enviados corretamente?
- Há uma resposta do servidor?
- O estado `salvando` está sendo atualizado corretamente?

---

### 5. Solução Completa
Aqui está um exemplo completo de como o código deve funcionar:

#### Backend (Exemplo de Rota):
```javascript
app.post('/api/agricultura/salvar', async (req, res) => {
    try {
        const dados = req.body;
        // Lógica para salvar os dados no banco de dados
        console.log("Dados recebidos:", dados);
        res.status(200).json({ success: true, message: "Dados salvos com sucesso!" });
    } catch (error) {
        console.error("Erro ao salvar dados:", error);
        res.status(500).json({ success: false, message: "Erro ao salvar dados." });
    }
});
```

#### Frontend (React):
```tsx
import { useState } from 'react';

function Admin() {
    const [salvando, setSalvando] = useState(false);

    const handleSalvar = async () => {
        setSalvando(true);

        try {
            const dados = { /* seus dados aqui */ };
            const resultado = await salvarDadosAgricultura(dados);
            alert(resultado.message || "Dados salvos com sucesso!");
        } catch (error) {
            alert(error.message || "Erro ao salvar dados. Tente novamente.");
        } finally {
            setSalvando(false);
        }
    };

    return (
        <button
            onClick={handleSalvar}
            disabled={salvando}
            className="inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0 bg-primary text-primary-foreground hover:bg-primary/90 h-10 px-4 py-2 w-full"
            type="submit"
        >
            {salvando ? (
                <span className="flex items-center gap-2">
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="24"
                        height="24"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        className="lucide lucide-loader-circle h-4 w-4 animate-spin"
                    >
                        <path d="M21 12a9 9 0 1 1-6.219-8.56"></path>
                    </svg>
                    Salvando...
                </span>
            ) : (
                "Salvar"
            )}
        </button>
    );
}
```

---

### 6. Conclusão
Se o problema persistir após essas verificações, compartilhe mais detalhes do código (backend e frontend) para que eu possa ajudar a identificar o problema específico. 😊