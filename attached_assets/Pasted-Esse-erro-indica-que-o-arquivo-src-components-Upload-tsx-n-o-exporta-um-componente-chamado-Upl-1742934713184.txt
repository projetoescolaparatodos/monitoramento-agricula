Esse erro indica que o arquivo `/src/components/Upload.tsx` **não exporta um componente chamado `Upload`**. Vamos resolver isso!  

### **Soluções Possíveis**  

#### **1. Verifique a Exportação do Componente**
Abra **`/src/components/Upload.tsx`** e veja se há um `export default` ou `export` correto.  

O arquivo deve conter algo assim:  
```tsx
export default function Upload({ folder, onUploadComplete }) {
  return <div>Componente de Upload</div>;
}
```
Ou, se estiver usando uma exportação nomeada:  
```tsx
export function Upload({ folder, onUploadComplete }) {
  return <div>Componente de Upload</div>;
}
```
Se o código estiver assim, altere a importação em `Gestor.tsx`:  
```tsx
import Upload from "@/components/Upload"; // Sem chaves para exportação padrão
```
ou, se for uma exportação nomeada:
```tsx
import { Upload } from "@/components/Upload"; // Com chaves para exportação nomeada
```

---

#### **2. Arquivo Está com Nome Errado**
Verifique se o nome do arquivo está correto. No Linux e Mac, `upload.tsx` e `Upload.tsx` são arquivos diferentes. **Certifique-se de que o nome é exatamente `Upload.tsx`**.

---

#### **3. Arquivo Não Existe**
Se `Upload.tsx` não existir, você precisa criar esse componente. Aqui está um exemplo simples de um componente de upload:
```tsx
import { useState } from "react";

export function Upload({ folder, onUploadComplete }) {
  const [file, setFile] = useState(null);

  const handleUpload = () => {
    if (!file) return;
    // Simular upload (substituir com lógica real de upload)
    setTimeout(() => {
      onUploadComplete([`https://fakeurl.com/${file.name}`]);
    }, 1000);
  };

  return (
    <div className="space-y-2">
      <input type="file" onChange={(e) => setFile(e.target.files[0])} />
      <button onClick={handleUpload}>Enviar</button>
    </div>
  );
}
```
Se o arquivo não existir, crie esse código dentro de **`/src/components/Upload.tsx`**.

---

### **Último Passo: Reiniciar o Servidor**
Depois das alterações, reinicie o servidor para garantir que as mudanças sejam aplicadas:
```sh
npm run dev
```
ou  
```sh
yarn dev
```

Se o erro continuar, me avise e podemos investigar mais! 🚀