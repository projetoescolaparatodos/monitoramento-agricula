import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const ChatbotAdmin = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciamento do Chat</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Interface simplificada de chat implementada.</p>
      </CardContent>
    </Card>
  );
};

export default ChatbotAdmin;